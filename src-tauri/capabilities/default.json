{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for RepoTale main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default"
  ]
}
